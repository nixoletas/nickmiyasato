# Build output
dist/
.astro/

# Résumé PDFs — build output that has to sit in public/ so `astro dev` serves
# it. Regenerate with `npm run build && npm run resume:pdf`.
public/*.pdf

# Dependencies
node_modules/

# Environment
.env
.env.production
.env.local
.env.*.local

# Editor / OS
.DS_Store
*.log
npm-debug.log*
.idea/

# Resume source. Deliberately untracked — this repo is public and the document
# carries a personal phone number. src/data/experience.ts mirrors its contents.
docs/
